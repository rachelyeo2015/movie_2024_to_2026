[project]
name = "project-3-movies"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "rachelyeo2015", email = "rachelyeo2015@gmail.com" }
]
requires-python = ">=3.11"
dependencies = [
    "numpy>=2.4.6",
    "openpyxl>=3.1.5",
    "pandas>=3.0.5",
    "pathlib>=1.0.1",
]

[project.scripts]
project-3-movies = "project_3_movies:analyse_movies"

[build-system]
requires = ["uv_build>=0.12.3,<0.13.0"]
build-backend = "uv_build"
